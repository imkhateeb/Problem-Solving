#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, w; cin >> n >> w;
    vector<pair<int, int>> v(n);

    for(int i = 0; i < n; i++){
        int x, y; cin >> x >> y;
        v[i] = {x, y};

        vector<vector<int>> dp(n, vector<int>(w+1, -1));

        l
    }
    return 0;
}